interface RowType {
  name: string;
  id: string;
  city: string;
  slow:string
  status: number;
  fast:string;
  now: string;
  fault: string;
  person: string;
  tel: string;
}

export type {
  RowType,
}
